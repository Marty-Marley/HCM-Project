# import * from './generated/prisma.graphql'

type Mutation {
  createEmployee(name: String!, email: String!, age: Int, avatar: String, entitlements: [Entitlements]!): Employee!
}

type Query {
  employees: [Employee]!
}