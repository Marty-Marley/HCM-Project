enum Entitlements {
  PROFILE
  HOLIDAYS
  TIMESHEET
  TEAM
}

# type User {
#   id: ID! @unique
#   name: String!
#   email: String!
#   age: Int
#   avatar: String
# }

type Employee {
  id: ID! @unique
  name: String!
  email: String!
  age: Int
  avatar: String
  entitlements: [Entitlements]!
}