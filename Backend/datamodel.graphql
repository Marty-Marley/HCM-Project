enum Entitlements {
  PROFILE
  HOLIDAYS
  TIMESHEET
  TEAM
}

# enum Permissions {
#   MANAGER
#   EMPLOYEE
#   INTERN
# }

# type User {
#   id: ID! @unique
#   name: String!
#   email: String!
#   password: String!
#   age: Int
#   avatar: String
# }

type Employee {
  id: ID! @unique
  name: String!
  email: String!
  age: Int
  avatar: String
  entitlements: [Entitlements]!
}

# TODO
# There needs to be a user for permissions and stuff on login
# You need to query user for login
# Its not an employee that has entitlements, its the user
# Need permissions of manager and eployee